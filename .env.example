# Required host path to your Traefik dynamic file for Docker compose bind mount.
# Example: /home/zeeshan/self-hosted/traefik-stack/config/dynamic-bak.yml
DYNAMIC_CONFIG_PATH=

# Optional auth. If both are set, login is required.
# If either is missing, auth is disabled.
AUTH_USERNAME=
AUTH_PASSWORD=
