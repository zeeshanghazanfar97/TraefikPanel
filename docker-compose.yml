services:
  traefik-panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: traefik-panel
    # ports:
    #   - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DYNAMIC_CONFIG_PATH=${DYNAMIC_CONFIG_PATH:?DYNAMIC_CONFIG_PATH must be set in .env}
    networks:
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefikpanel.rule=Host(`traefikpanel.zeeit.dev`)"
      - "traefik.http.routers.traefikpanel.entrypoints=websecure"
      - "traefik.http.routers.traefikpanel.tls.certresolver=cloudflare"
      # - "traefik.http.routers.traefikpanel.middlewares=authentik@file"
      - "traefik.http.services.traefikpanel.loadbalancer.server.port=3000"
    volumes:
      - ${DYNAMIC_CONFIG_PATH:?DYNAMIC_CONFIG_PATH must be set in .env}:${DYNAMIC_CONFIG_PATH:?DYNAMIC_CONFIG_PATH must be set in .env}
    restart: unless-stopped

networks:
  traefik-public:
    external: true
